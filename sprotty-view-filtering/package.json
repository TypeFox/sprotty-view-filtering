{
    "name": "sprotty-view-filtering",
    "description": "Please enter a brief description here",
    "scripts": {
        "build": "esbuild ./src/index.ts --bundle --sourcemap --outfile=./static/index.js",
        "watch": "npm run build -- --watch",
        "start": "npm run build -- --serve=8080 --servedir=static"
    },
    "devDependencies": {
        "@types/node": "^18.0.0",
        "esbuild": "^0.17.8",
        "typescript": "4.9.5"
    },
    "dependencies": {
        "reflect-metadata": "^0.1.13",
        "sprotty": "next",
        "sprotty-elk": "next"
    }
}
